# Copy this file to .env and fill in your values. Never commit .env to git.
# Optional: pip install python-dotenv and load .env from app/scripts, or export vars in your shell.

# --- Database (ProxyForge app and hydrators) ---
# No spaces around =. If password contains # or spaces, use double quotes: MYSQL_PASSWORD="p@ss#word"
MYSQL_HOST=127.0.0.1
MYSQL_USER=your_mysql_user
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=wargaming_erp

# --- MyMiniFactory (fetcher, backfill_stl_images) ---
MMF_USERNAME=your_myminifactory_username
# One of:
#   MMF_API_KEY=your_oauth2_access_token
#   MMF_SESSION_COOKIE=full_cookie_header_from_browser
# Session cookie is required for data-library API with Cloudflare; use curl_cffi (pip install curl_cffi).

# Optional MMF fetcher tuning
# MMF_LIBRARY_SOURCE=data_library
# MMF_ENRICH_PREVIEW=1
# MMF_ENRICH_IMAGES=1
# MMF_ENRICH_DELAY=0.3
